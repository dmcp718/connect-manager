services:
  guide:
    build: .
    container_name: wrkflw-guide
    restart: unless-stopped
    ports:
      - "8080:8080"
    networks:
      - caddy_shared

networks:
  caddy_shared:
    external: true
